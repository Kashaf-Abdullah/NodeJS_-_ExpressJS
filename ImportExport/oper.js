//when function define so it is parameter
// when function all so it is argument

const add=(a,b)=>{
return a+b;
}


const myName=(name)=>{
    return name;
}

module.exports=add;
module.exports=myName

// module.exports={add,myName}